<?= $this->extend('layouts/main') ?>
<?= $this->section('content') ?>
<div class="container py-4"><?php if (session('success')): ?><div class="alert alert-success"><?= esc(session('success')) ?></div><?php endif ?><div class="d-flex justify-content-between gap-3 mb-4"><div><h1 class="h3 mb-1">Oportunidade</h1><code><?= esc($opportunity['correlation_id']) ?></code></div><a class="btn btn-outline-secondary align-self-start" href="<?= site_url('vendedor-eventual') ?>">Voltar</a></div><div class="alert alert-primary"><strong>Objetivo desta jornada:</strong> ajudar qualquer empregado habilitado a identificar a necessidade, consultar a situação do cliente, aplicar o diagnóstico e encaminhar a solução adequada sem perder sua autoria.</div><div class="row g-4"><div class="col-12 col-lg-5"><div class="card mb-4"><div class="card-header">Dados iniciais</div><div class="card-body"><dl class="mb-0"><dt>CNPJ</dt><dd><?= esc($opportunity['cnpj']) ?></dd><dt>Campanha</dt><dd><?= esc($opportunity['campaign_name']) ?></dd><dt>Canal</dt><dd><?= esc($opportunity['channel']) ?></dd><dt>Estado</dt><dd><?= esc($opportunity['status']) ?></dd><dt>Questionário vinculado</dt><dd><?= esc($opportunity['questionnaire_version'] ?? 'nenhum publicado no registro') ?></dd><dt>Contexto</dt><dd class="mb-0"><?= nl2br(esc($opportunity['contact_context'])) ?></dd></dl></div></div><div class="card"><div class="card-header">Situação na carteira</div><div class="card-body"><div class="alert alert-<?= $opportunity['portfolio']['state'] === 'assigned' ? 'warning' : ($opportunity['portfolio']['state'] === 'unavailable' ? 'secondary' : 'success') ?> mb-3"><strong><?= esc($opportunity['portfolio']['label']) ?></strong></div><?php if ($opportunity['portfolio']['state'] === 'assigned'): ?><dl class="mb-3"><dt>Responsável</dt><dd><?= esc($opportunity['portfolio']['responsible_name'] ?? 'Não informado') ?></dd><dt>Unidade</dt><dd><?= esc($opportunity['portfolio']['responsible_unit'] ?? 'Não informada') ?></dd><dt>Estado operacional</dt><dd><?= esc($opportunity['portfolio']['operational_status'] ?? 'Não informado') ?></dd></dl><p class="small text-muted mb-0">A existência de carteira não bloqueia o atendimento. O responsável poderá ser envolvido na continuidade.</p><?php elseif ($opportunity['portfolio']['state'] === 'unavailable'): ?><p class="small text-muted mb-0">O registro da oportunidade permanece disponível mesmo sem integração com a carteira.</p><?php else: ?><p class="small text-muted mb-0">A oportunidade pode prosseguir e será usada posteriormente para solicitar inclusão provisória em carteira.</p><?php endif ?></div></div></div><div class="col-12 col-lg-7"><div class="card mb-4"><div class="card-header">Próximas ações comerciais</div><div class="card-body"><ol class="mb-3"><li>Confirmar os dados públicos da empresa.</li><li>Aplicar o questionário de diagnóstico.</li><li>Apresentar até três soluções recomendadas e registrar o interesse.</li></ol><div class="d-flex gap-2"><a class="btn btn-primary" href="<?= site_url('vendedor-eventual/campanhas/' . $opportunity['campaign_id'] . '/catalogo') ?>">Consultar catálogo</a><button class="btn btn-outline-secondary" disabled>Iniciar diagnóstico — próxima etapa</button></div></div></div><div class="card"><div class="card-header">Linha do tempo</div><div class="list-group list-group-flush"><?php foreach ($opportunity['events'] as $event): ?><div class="list-group-item"><div class="d-flex justify-content-between gap-3"><strong>Oportunidade registrada</strong><small><?= esc(date('d/m/Y H:i', strtotime($event['occurred_at']))) ?></small></div><small class="text-muted">Evento <?= esc($event['event_id']) ?> · canal <?= esc($event['channel']) ?><?php if ($event['content_version']): ?> · questionário <?= esc($event['content_version']) ?><?php endif ?></small></div><?php endforeach ?></div></div></div></div></div>
<?= $this->endSection() ?>
